test:
  pre:
    - redis-server&
  override:
    - python3 -m unittest test.py
  post:
    - killall redis-server
deployment:
  development:
    branch: development
    heroku:
      appname: twogifs-development
  production:
    branch: master
    heroku:
      appname: twogifs
